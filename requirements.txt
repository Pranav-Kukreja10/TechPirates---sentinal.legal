# ── Sentinal Legal Engine v3.0 — Enterprise Requirements ────────────────────
# Install with: pip install -r requirements.txt
 
# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.1
python-dotenv==1.0.1
 
# Async HTTP
httpx==0.27.0
 
# AI Clients
groq==0.9.0
 
# ── ENCRYPTION ─────────────────────────────────────────────────────────────
# E2E: X25519 ECDH + HKDF + AES-256-GCM
cryptography==42.0.8
 
# ── OPTIONAL: NLP ENHANCEMENT ──────────────────────────────────────────────
# Uncomment for production-grade entity extraction (replaces regex fallback)
# spacy==3.7.4
# python -m spacy download en_core_web_sm
 